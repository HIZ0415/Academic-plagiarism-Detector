% 管理维护模块核心领域类图
\begin{tikzpicture}[
  class/.style={
    draw,
    rectangle split,
    rectangle split parts=3,
    rectangle split part align={center,left,left},
    align=left,
    text width=2.65cm,
    font=\scriptsize,
    inner sep=2.5pt
  },
  rel/.style={-{Stealth[length=1.6mm]},semithick},
  dep/.style={-{Stealth[length=1.6mm]},dashed,semithick}
]
  \node[class] (admin) at (0,0) {
    \textbf{管理员}
    \nodepart{second}
    - 角色, 权限
    \nodepart{third}
    + 维护实体()\\
    + 查看统计()
  };

  \node[class] (account) at (4.2,1.6) {
    \textbf{用户账号}
    \nodepart{second}
    - 状态, 权限级别
    \nodepart{third}
    + 更新状态()\\
    + 分配权限()
  };

  \node[class] (resource) at (4.2,0) {
    \textbf{学术资源}
    \nodepart{second}
    - 类型, 分类标签
    \nodepart{third}
    + 添加资源()\\
    + 分类资源()
  };

  \node[class] (model) at (4.2,-1.6) {
    \textbf{模型配置}
    \nodepart{second}
    - 模型类型, 状态
    \nodepart{third}
    + 更新配置()\\
    + 切换模型()
  };

  \node[class] (log) at (8.4,1.6) {
    \textbf{系统日志}
    \nodepart{second}
    - 类型, 时间
    \nodepart{third}
    + 记录操作()\\
    + 审计追踪()
  };

  \node[class] (stat) at (8.4,0) {
    \textbf{统计报表}
    \nodepart{second}
    - 维度, 格式
    \nodepart{third}
    + 生成图表()\\
    + 导出报表()
  };

  \node[class] (complaint) at (12.6,1.6) {
    \textbf{举报处理记录}
    \nodepart{second}
    - 方式, 结论
    \nodepart{third}
    + 审核举报()\\
    + 通知用户()
  };

  \draw[rel] (admin) -- (account);
  \draw[rel] (admin) -- (resource);
  \draw[rel] (admin) -- (model);
  \draw[rel] (account) -- (log);
  \draw[rel] (resource) -- (stat);
  \draw[rel] (model.east) -- ++(0.8,0) |- (stat.south);
  \draw[dep] (complaint) -- (log);
\end{tikzpicture}
