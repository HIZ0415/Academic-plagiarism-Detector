% 学术内容检测模块检测引擎类图
\begin{tikzpicture}[
  class/.style={
    draw,
    rectangle split,
    rectangle split parts=3,
    rectangle split part align={center,left,left},
    align=left,
    text width=2.85cm,
    font=\scriptsize,
    inner sep=2.5pt
  },
  rel/.style={-{Stealth[length=1.6mm]},semithick},
  dep/.style={-{Stealth[length=1.6mm]},dashed,semithick}
]
  \node[class] (service) at (0,0) {
    \textbf{内容检测服务}
    \nodepart{second}
    - 任务, 内容类型
    \nodepart{third}
    + 分发任务()\\
    + 汇总结果()
  };

  \node[class] (imageEngine) at (4.4,1.6) {
    \textbf{图像鉴伪检测器}
    \nodepart{second}
    - 图像特征, 区域
    \nodepart{third}
    + 检测伪造()\\
    + 标注异常()
  };

  \node[class] (paperEngine) at (4.4,0) {
    \textbf{论文AIGC检测器}
    \nodepart{second}
    - 段落, 事实线索
    \nodepart{third}
    + 识别AI段落()\\
    + 生成说明()
  };

  \node[class] (reviewEngine) at (4.4,-1.6) {
    \textbf{Review检测器}
    \nodepart{second}
    - 评审文本, 模板
    \nodepart{third}
    + 检测虚假评审()\\
    + 识别模板倾向()
  };

  \node[class] (fusion) at (8.8,0) {
    \textbf{结果融合分析器}
    \nodepart{second}
    - 子结果, 权重
    \nodepart{third}
    + 融合评分()\\
    + 生成综合结论()
  };

  \node[class] (report) at (13.2,0.8) {
    \textbf{报告生成器}
    \nodepart{second}
    - 模板, 输出格式
    \nodepart{third}
    + 生成PDF()\\
    + 生成HTML()
  };

  \node[class] (explain) at (13.2,-0.8) {
    \textbf{可视化解释器}
    \nodepart{second}
    - 标注, 概率
    \nodepart{third}
    + 生成解释视图()\\
    + 输出图表数据()
  };

  \draw[rel] (service) -- (imageEngine);
  \draw[rel] (service) -- (paperEngine);
  \draw[rel] (service) -- (reviewEngine);
  \draw[rel] (imageEngine) -- (fusion);
  \draw[rel] (paperEngine) -- (fusion);
  \draw[rel] (reviewEngine) -- (fusion);
  \draw[dep] (fusion) -- (report);
  \draw[dep] (fusion) -- (explain);
\end{tikzpicture}
