% 用户展示模块核心领域类图
\begin{tikzpicture}[
  class/.style={
    draw,
    rectangle split,
    rectangle split parts=3,
    rectangle split part align={center,left,left},
    align=left,
    text width=2.65cm,
    font=\scriptsize,
    inner sep=2.5pt
  },
  rel/.style={-{Stealth[length=1.6mm]},semithick}
]
  \node[class] (user) at (0,0) {
    \textbf{用户}
    \nodepart{second}
    - 用户ID, 状态
    \nodepart{third}
    + 查看资料()\\
    + 查看消息()
  };

  \node[class] (profile) at (4.2,1.6) {
    \textbf{个人资料}
    \nodepart{second}
    - 头像, 等级
    \nodepart{third}
    + 展示信息()\\
    + 同步资料()
  };

  \node[class] (record) at (4.2,0) {
    \textbf{检测记录}
    \nodepart{second}
    - 类型, 状态
    \nodepart{third}
    + 筛选记录()\\
    + 查看详情()
  };

  \node[class] (review) at (4.2,-1.6) {
    \textbf{审核记录}
    \nodepart{second}
    - 进度, 结论
    \nodepart{third}
    + 查看进度()\\
    + 终止任务()
  };

  \node[class] (feedback) at (8.4,0.8) {
    \textbf{反馈消息}
    \nodepart{second}
    - 类型, 已读状态
    \nodepart{third}
    + 展示反馈()\\
    + 标记已读()
  };

  \node[class] (report) at (8.4,-0.8) {
    \textbf{报告文件}
    \nodepart{second}
    - 格式, 下载地址
    \nodepart{third}
    + 校验权限()\\
    + 生成链接()
  };

  \draw[rel] (user) -- (profile);
  \draw[rel] (user) -- (record);
  \draw[rel] (user) -- (review);
  \draw[rel] (record) -- (feedback);
  \draw[rel] (record) -- (report);
  \draw[rel] (review) -- (report);
\end{tikzpicture}
