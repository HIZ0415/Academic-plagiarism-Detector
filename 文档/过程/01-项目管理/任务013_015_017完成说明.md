# 任务013、015、017完成说明

日期：2026.5.5

## 任务013：后端-图像检测链路

**要求：** 保持现有图像检测任务创建、调度、落库链路可用

**完成项**

1. 图像检测链路（`submit_detection2` → Celery `fetch_batch` → `process_single_result` → `finalize_task` → 生成PDF报告）全链路保持不变，未做任何破坏性改动。
2. 优化 `get_task_detail_unified` 中的 `detail_endpoint` 映射，原三目嵌套改为 dict 查表，可读性更好且新增类型时只需加一行。
   参考：views_dectection.py (line 746~752)

**验证方式：** 现有图像检测流程与改动前行为完全一致，`submit_detection2` → 轮询 `get_detection_task_status_normal` → `list_task_results` 全链路无变化。

---

## 任务015：后端-论文检测链路

**要求：** 打通论文任务创建、异步执行、结果落库、状态查询链路

**完成项**

1. 新增 `core/tasks_paper.py`，包含两个 Celery 异步任务：
   - `run_paper_aigc_detection`：论文 AIGC 检测。读取预处理产物 → 计算段落级 AI 贡献评分 → 结果写入 JSON → 标记 completed → 发送系统通知。
     参考：tasks_paper.py (line 153~189)
   - `run_resource_check_detection`：学术资源/引用检测。提取参考文献 → 检查 DOI 完整性 → 结果写入 JSON → 标记 completed。
     参考：tasks_paper.py (line 237~268)
2. 改写 `views_paper.py` 的提交接口 `submit_aigc_task` / `submit_resource_check_task`，由原来的同步模拟改为 `Celery.delay()` 派发异步任务。
   参考：views_paper.py (line 195~253)
3. 改写 `get_paper_task_status`，去掉原来基于 `elapsed_seconds` 的时间模拟，改为直接读取数据库 `DetectionTask.status`。
   参考：views_paper.py (line 260~280)
4. 改写 `get_aigc_result` / `get_resource_check_result`，从 Celery 落盘的 JSON 文件读取结果，而非内存临时计算。
   参考：views_paper.py (line 298~327)

**状态流转：** pending → in_progress → completed / failed（与图像检测链路状态机完全一致）

---

## 任务017：后端-Review检测链路

**要求：** 打通 Review 任务创建、异步执行、结果落库、状态查询链路

**完成项**

1. 新增 Celery 异步任务 `run_review_detection`：读取 Review 预处理文本 → 计算整体 AI 生成概率与模板化倾向 → 逐句风险评分 → 结果写入 JSON → 标记 completed。
   参考：tasks_paper.py (line 297~340)
2. 改写 `submit_review_detection_task`，提交后 `Celery.delay()` 派发异步任务，取代原来的同步模拟。
   参考：views_paper.py (line 334~409)
3. 改写 `get_review_task_status`，直接读数据库状态。
   参考：views_paper.py (line 416~434)
4. **新增** `get_review_detection_result` 接口 `GET /api/review/<task_id>/result/`，返回 Review 检测结果。
   参考：views_paper.py (line 441~475)
5. 在 `urls.py` 注册新路由 `review/<int:task_id>/result/`。
   参考：urls.py (line 81)
6. 更新 `get_task_detail_unified` 中 Review 的 `detail_endpoint`，由 `review/tasks/{taskId}/status` 修正为 `review/{taskId}/result`。
   参考：views_dectection.py (line 750)

**新增接口文档**

```
GET /api/review/<task_id>/result/
Authorization: Bearer <token>

成功响应（200）：
{
  "task_id": 123,
  "overall_ai_probability": 0.62,
  "overall_risk_level": "medium",    // high / medium / low
  "template_score": 0.38,
  "is_template_like": false,
  "summary": "检测完成：AI 生成概率约 62.0%，未检测到明显模板化倾向，高风险句 3 句。",
  "sentence_count": 15,
  "high_risk_count": 3,
  "sentences": [
    {"index": 1, "text": "...", "ai_probability": 0.78, "risk_level": "high"},
    ...
  ]
}

未完成（202）：{"detail": "任务尚未完成。", "status": "in_progress"}
失败（400）：  {"detail": "任务执行失败。", "error_message": "..."}
不存在（404）：{"detail": "任务不存在。"}
```

---

## 本次改动文件

| 文件 | 位置 | 操作 |
|------|------|------|
| `tasks_paper.py` | `core/tasks_paper.py` | **新增** — 论文/Review 三种检测的 Celery 异步任务 |
| `views_paper.py` | `core/views/views_paper.py` | **整文件替换** — 接入异步调度 + 新增 Review 结果接口 |
| `urls.py` | `core/urls.py` | **修改** — 新增1条 import + 1条路由 |
| `views_dectection.py` | `core/views/views_dectection.py` | **修改** — 优化 detail_endpoint 映射 |

## 已验证

1. `python -m py_compile` 通过：tasks_paper.py、views_paper.py、urls.py、views_dectection.py
2. 无数据库迁移变更（未修改 models.py）
3. 本地 `CELERY_TASK_ALWAYS_EAGER = True` 模式下任务同步执行正常
4. 图像检测链路无任何行为变化（任务013兼容性验证）

## 备注

- 三种检测结果均为**确定性 mock**（基于 SHA256/MD5 哈希），同样输入始终产生相同结果，Alpha 阶段足够使用。后续接入真实 AI 服务时，只需替换 `tasks_paper.py` 中的 `_build_*_result` 函数。
- 生产部署时 Celery worker 需监听 `cpu` 队列：`celery -A fake_image_detector worker -Q cpu,default,ai`。本地开发无需额外配置。
