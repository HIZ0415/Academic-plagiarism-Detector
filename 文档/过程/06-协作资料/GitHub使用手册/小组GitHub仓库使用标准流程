# 小组GitHub仓库使用标准流程
> *Author:Su*
> *latest update:2026.4.3*
## 前置：在vscode中准备好git
+ 在vscode图形界面操作：在vscode左侧工具栏点击源代码管理（git图标），点击克隆仓库，上方命令行会要求输出仓库名称，把仓库链接粘贴到上方。（这步只需要做一次，等待联网拉取仓库结束）
+ 在终端拉取：
```Bash
git clone https://github.com/HIZ0415/Academic-plagiarism-Detector.git
cd Academic-plagiarism-Detector
git fetch origin
git switch --track origin/feature/你的个人分支名
```
+ 仓库链接：https://github.com/HIZ0415/Academic-plagiarism-Detector.git

#

## 每次的修改流程如下：
### 1. 开发前准备（更新到最新的dev分支，防止代码版本落后，减少PR冲突）终端执行如下指令：
```Bash
git switch dev
git pull origin dev

git switch feature/memberA
git merge dev
```
### 2. 代码修改完成准备提交到个人分支：
```Bash
git add .
git commit -m "修改信息"
git push origin feature/你的个人分支名
```
### 3. 从个人分支合并到`dev`分支（完成某功能、修复了bug等之后）：
1. 在GitHub上进入项目仓库，点击右上角`pull requrests(PR)` -> `New pull request`
![alt text](仓库主界面.png)
![alt text](<new pull requrest.png>)
![alt text](compare.png)
2. `base`栏选`dev`，`compare`栏选自己修改之后的个人分支（比如`feature/Su`）。选中之后应当出现如下图内容，点击`Create pull request`
![alt text](<Create New Request.png>)
3. 在出现的对话框中填写适当的说明文字，方便其他同学review（对话框支持markdown语法）。添加完成之后点击下边的`Create pull request`
> 建议写`1.这次做了什么；2.改了哪些文件；3.怎么测试的；4.有无已知问题`。

![alt text](<add description.png>)
4. 在跳转的界面可以看到自己当前提交的相关信息，在右侧` Reviewers`栏选择需要审核的同学
![alt text](<add reviewers.png>)
5. 通知自己添加了review权限的同学

### 4.review批准合并
1.审核者同样在`PR`界面，点击进入下方出现的`new pull request`
![alt text](reviewer_PR.jpg)
+ 点击上方`file changed`栏，下方可以浏览已提交请求合并的更改，确认***修改无误***之后点击`submit review`，填写对应的说明之后选择`Approve`选项，然后点击`submit review`
![alt text](<file changed.jpg>)
![alt text](approve.png)


### tips：你提出PR之后，其他人修改的部分合并进了dev分支（重要！！！）
+ 比如我开了 `feature/Su -> dev` 的 PR，但这时 聂、翟 的代码先合进 `dev` 了。那我就需要把最新 `dev` 再同步到自己的个人分支。
```Bash
git switch dev
git pull origin dev

git switch feature/memberA
git merge dev
```
+ 如果没有冲突，则执行`git push origin feature/memberA`，PR会自动合并