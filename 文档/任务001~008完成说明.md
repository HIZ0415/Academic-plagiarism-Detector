# 任务001~008完成说明

日期：2026.4.28

**完成项**

1. 001 统一任务模型 task_type：沿用现有 DetectionTask.task_type，并在新增 Review 任务提交时显式写入 review_detection。
   参考：models.py (line 276)、views_paper.py (line 594)
2. 002 统一任务状态机：在任务中心查询侧增加状态白名单校验（pending/in_progress/completed/failed）。
   参考：views_dectection.py (line 13)、views_dectection.py (line 643)
3. 003 统一历史任务查询接口：保持 user-tasks 公共字段输出（含 task_type），并补状态过滤合法性。
   参考：views_dectection.py (line 630)
4. 004 统一详情聚合接口：新增 GET /tasks/{task_id}/detail/，按 task_type 返回统一公共字段+差异化结果。
   参考：views_dectection.py (line 680)、urls.py (line 57)
5. 005 Alpha 文件白名单：在上传入口收敛为最小格式范围。
   参考：views_imageupload.py (line 14)、views_paper.py (line 19)
6. 006 图像链路仅 .png/.jpg/.jpeg：拒绝其他扩展名，不再让 pdf/zip 进入主图像上传流程。
   参考：views_imageupload.py (line 30)、views_imageupload.py (line 49)
7. 007 Review 仅文本或 .txt：新增 POST /review/submit/（支持 text 或 file=.txt）与状态查询接口。
   参考：views_paper.py (line 541)、views_paper.py (line 617)、urls.py (line 79)
8. 008 论文链路仅 .pdf：移除 docx/txt 放行。
   参考：views_paper.py (line 351)

**本次改动文件**

1. views_paper.py
2. views_imageupload.py
3. views_dectection.py
4. urls.py

**已验证**

1. python -m py_compile 通过：views_paper.py, views_imageupload.py, views_dectection.py, urls.py

## 任务009：论文与专家人工 Review 预处理流程

**变更内容**

1. 新增 `core/utils/paper_preprocessing.py`，论文上传后仅处理 PDF：提取文本、清洗、段落切分，并生成 `paper-preprocess-v1` AI 输入。
2. 新增 `core/utils/review_preprocessing.py`，专家人工 Review 支持在线文本和 `.txt`：完成编码标准化、BOM 清理、控制字符清理、空白归一化，并生成 `review-preprocess-v1` AI 输入。
3. 更新 `views_paper.py`，保存原始文本、清洗后文本、段落/AI 输入 JSON，并在任务提交前校验预处理产物。
4. 更新 `requirements.local.txt`，补充 PDF 预处理依赖 `PyMuPDF`。
5. 更新 `scripts/start-local-windows.ps1`，依赖探针增加 `fitz`，避免已有虚拟环境漏装 PyMuPDF。

**已验证**

1. `python -m py_compile` 通过：`paper_preprocessing.py`、`review_preprocessing.py`、`views_paper.py`。
2. 本地 Review 预处理测试通过：在线文本、`utf-8-sig` TXT、`gbk` TXT。
3. 本地 PDF 预处理测试通过：内存 PDF 文本提取、清洗、段落切分、AI payload。
4. 负向测试通过：空 Review 文本拒绝、非法 PDF 字节拒绝。
